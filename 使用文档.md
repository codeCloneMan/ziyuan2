# 字源形码 - 使用文档

## 项目概述

基于「字源1.31版字根」的专业字根记忆训练平台，使用 React + TypeScript + Vite + Tailwind CSS + shadcn/ui 构建。

## 功能页面

### 1. 首页
项目介绍和快速导航。

### 2. 字根练习
深度吸收宇浩码和虎码练习方式的字根记忆训练系统，功能包括：

#### 练习模式
- **渐进学习**：科学记忆，逐步掌握全部字根
- **错题回顾**：针对错题，查漏补缺
- **简体练习**：科学记忆，逐步掌握简体字根

#### 分阶段练习路线
- **入门模式**：每次5个新字根，全部掌握后再加入
- **渐进模式**：按难度递增，先练高频字根
- **全码模式**：所有字根随机出题
- **弱项攻坚**：专门练习错误率最高的字根
- **速度挑战**：限时60秒答题，追求速度和连击

#### 交互式字根键盘
- 显示26键完整键盘布局，每个键位上显示对应字根
- 当前出题字根在键盘上高亮闪烁提示
- 已掌握字根和未掌握字根用不同透明度区分
- 点击键位可查看该键所有字根
- 支持显示模式切换（字根模式/编码模式/空白模式）

#### 艾宾浩斯记忆增强
- 首次出现字根：显示编码提示，3秒后自动消失
- 答错处理：错误字根进入"紧急复习队列"，在后续3-5题内必定重复出现
- 遗忘曲线跟踪：记录每个字根的答题时间、正确率
- 连击加倍积分：正确+10分，连击加倍（最高10倍），错误-5分

#### 练习进度看板
- 已练习/总数、已掌握数、今日练习量、连续正确数、积分系统
- 掌握进度条（渐变色可视化）
- 最弱字根TOP10排名
- 近7日练习量柱状图

#### 字根-例字关联
- 答对后显示当前字根的2-3个例字及完整编码

#### 拼音音托提示
- 答错时显示字根的读音和对应键位的关系（如"高 (Gāo → g)"）

### 3. 词组练习
基于字源编码的词组输入训练系统，内置 **850+** 个常用词组（含双字词、三字词、四字词和成语）。

#### 练习模式
- **双字词**：练习常用双字词组编码
- **三字词**：练习常用三字词组编码
- **四字词**：练习四字词和成语编码
- **混合词组**：混合练习双字和三字词组
- **短句练习**：练习常用短句的连续输入

#### 编码规则
- 双字词：各取全码
- 三字词：取首字首码 + 次字首码 + 第三字全码
- 多字词：取前两字首码 + 最后一字全码

#### 练习流程
1. 选择练习模式（双字词/三字词/混合/短句）
2. 点击开始练习，每次随机抽取20个词组
3. 根据显示的词组输入完整编码
4. 按Enter或点击确认提交答案
5. 系统自动判断对错并显示：
   - 每个字的独立编码（提示区）
   - 完整词组编码（绿色=正确，红色=错误）
6. 练习完成后显示统计：
   - 正确率、打字速度（字/分钟）、选重率、平均用时
   - 每题的详细结果和对错分析

---

### 4. 整字练习
深度吸收宇浩码和虎码练习方式的整字编码训练系统，功能包括：

#### 练习模式
- **全部汉字**：科学记忆，逐步掌握全部汉字
- **常用500字**：科学记忆，逐步掌握前500常用字

#### 拆分可视化
- 答错时可视化显示汉字的拆分过程（如：国 → 囗 + 王 + 丶）
- 拆分动画：字根逐个出现，像搭积木一样

#### 字根提示系统
- 首次出现的汉字：显示完整拆分+编码提示
- 第二次出现：只显示拆分，不显示编码
- 第三次及以后：不显示任何提示
- 按空格键可以获取提示

#### 编码规则教学
- 练习时根据当前字的编码长度，标注编码规则类型（A/AB/ABb/ABCc/ABCD/ABCZ）
- 用蓝色标注大码位置，绿色标注小码位置
- 帮助用户理解编码规律

#### 常用字分级练习
- 常用500字：最核心的高频字
- 常用1000字：日常覆盖
- 常用1500字：进阶覆盖
- 全部字集：完整练习

#### 必拆字专项练习
- 专门练习拆分非直觉的常见汉字，这些字容易拆错

#### 拆分正误对比
- 答错时，同时显示错误编码（红色❌）和正确编码（绿色✅）

#### 进度与统计
- 已练习字数/总字数、今日练习量、积分系统
- 拆分错误率最高的字TOP10
- 近7日练习量柱状图

### 5. 字根表
完整字根映射表，按键位分组展示，支持搜索筛选。

### 6. 字根图
字根键盘布局可视化展示。

### 7. 码表测评
专业码表评测工具，吸收字劫(CHS)和行业标准评码规范。

#### 使用方式
- **上传码表**：点击或拖拽上传码表文件（支持 .txt/.mb/.csv/.yaml 等格式）
- **预置方案**：一键测评当前字源131方案，无需上传文件

#### 核心指标
| 指标 | 说明 | 参考标准 |
|------|------|----------|
| 字频加权码长 | 字频加权平均码长 | < 3.5 优秀 |
| 全码重码率 | 全码重码字数/总字数 | < 5% 优秀 |
| 出简重码率 | 出简后的重码率 | 越低越好 |
| 选重率 | 重码字词键选率（参考GB/T18031） | < 2% 优秀 |
| 当量 | 击键效率指标 = 码长×(1+选重率) | < 1.5 优秀 |
| 综合评分 | 基于以上指标的加权综合分 | 参考国标 |

#### 详细分析（Tab标签页）
- **重码分析**：按字频段统计重码、重码编码列表
- **码长分布**：柱状图+表格展示各码长占比
- **按键分析**：26键热力图、左右手比例、手指负担分布
- **字频分析**：按字频排名分段统计编码效率

#### 词组测评（新增）
基于内置高频词组列表（850+个），测试码表对词组的编码能力。

| 指标 | 说明 | 参考标准 |
|------|------|----------|
| 词组覆盖率 | 码表能编码的词组数/总词组数 | ≥ 90% 优秀 |
| 词组平均码长 | 词组中每个字的平均码元数 | < 6 优秀 |
| 词组重码率 | 重码词组数/已编码词组数 | < 5% 优秀 |
| 词组选重率 | 重码组中非首词比例 | < 2% 优秀 |

- 分类统计：二字词、三字词、四字词（含成语）分别统计
- 词组编码规则：每个字取首选（最短）编码拼接
- 覆盖率可视化：进度条+百分比+颜色标识

#### 评码算法
```
字频加权码长 = Σ(字频 × 该字码长) / Σ(字频)
全码重码率 = 重码字数 / 总编码字数 × 100%
出简重码率 = 出简后仍有重码的字数 / 总字数 × 100%
选重率 = Σ(重码字中非首字的字频) / Σ(所有字频) × 100%
当量 = 平均码长 × (1 + 选重率)
综合评分 = 40×码长得分 + 30×重码得分 + 20×选重得分 + 10×当量得分
词组覆盖率 = 可编码词组数 / 总词组数 × 100%
词组重码率 = 重码词组数 / 已编码词组数 × 100%
词组选重率 = 重码组中非首词数 / 已编码词组数 × 100%
```

### 8. 拆分查询
汉字拆分和编码查询工具。

### 9. 常见问题
常见问题解答。

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build
```

## 数据修复记录

### 2026-05-02 第六次修复（词组分类导出 + 词组扩充 + 词组测评功能）

#### 1. EvaluatePage.tsx 安全除法补充
- 补充 `getHeatColor` 中 `freq / maxKeyFreq` → `safeDivide(freq, maxKeyFreq)`，保持一致性
- 确认其他除法运算均已使用 `safeDivide` 或除以常量（安全）

#### 2. builtinPhrases.ts 词组分类导出
- `twoCharPhrases`：包含 `RAW_TWO_CHAR` + `RAW_EXTRA` 中二字词
- `threeCharPhrases`：包含 `RAW_THREE_CHAR` + `RAW_EXTRA` 中三字词
- `fourCharPhrases`：包含 `RAW_FOUR_CHAR` + `RAW_EXTRA` 中四字及以上词
- `phraseList`：全部词组合并清理后导出

#### 3. builtinPhrases.ts 词组数量扩充至 850+
- 二字词补充 100 个高频词（教育、科学、世界等）
- 三字词补充 37 个政策概念词（自动化、信息化、生产力等）
- 四字词/成语补充 28 个（社会主义现代化、解放思想、以人为本等）
- 总词组数从 754 增至 850+

#### 4. EvaluatePage.tsx 词组测评功能
- 新增 `PhraseTierResult` 接口：词组分段测评结果
- 新增词组覆盖率计算：基于码表字→编码映射，测试词组编码能力
- 新增词组平均码长统计：每个字取首选编码，计算总码元数/字数
- 新增词组重码率统计：编码相同的不同词组视为重码
- 新增词组选重率统计：重码组中非首词的比例
- 新增"词组测评" Tab 标签页：表格+覆盖率进度条可视化
- 更新底部汇总表：新增"词组测评"分类（6项指标）
- 更新复制报告：包含词组测评数据

### 2026-05-02 第五次修复（综合修复：除法安全 + 字集修正 + 词组去重 + 测评增强）

#### 1. EvaluatePage.tsx 安全计算修复
- **修复 `normalizeWeights()` 空数组除零**：添加 `weights.length === 0` 前置检查，避免 `1 / weights.length` 除零
- 确认所有除法运算均已使用 `safeDivide` 保护
- 确认 `calcScore` 函数已有 `poor - excellent` 除零保护
- 确认综合评分 `score / 100` 安全（score 已被限制在 0-100）

#### 2. builtinCharSets.ts 字集修正
- **`top500Chars`**：确认 500 个，0 重复，0 非汉字
- **`top1000Chars`**：确认 1000 个，0 重复，0 非汉字
- **`top3000Chars`**：确认 3000 个，0 重复，0 非汉字
- **修复 `calculateCoverage()` 参照集**：gb2312 改用 `gb2312All`(6763字)，gbk 改用 `gbk`(21003字)
- **修复 `isInGBK()` Unicode 范围**：移除 CJK 扩展B区(0x20000+)，该区属于 GB18030 而非 GBK
- **修复 `gbk` 字集构建**：从 `top3000Chars`（仅3000字）改为基于 CJK基本区+扩展A区 构建，目标21003字

#### 3. commonChars.ts 确认
- 数据从 builtinCharSets 导入，一致性已确认
- top500=500, top1000=1000, top1500=1500, top3000=3000

#### 4. builtinPhrases.ts 词组去重
- **去除 5 个源数据重复词组**：'比较'、'感情'、'希望'、'保护'、'法律'
- 去重后 **754 个词组**（0 重复），超过 500 个目标
- 分类：二字词 280 个、三字词 150+ 个、四字词/成语 200+ 个、补充词组若干

#### 5. PhrasePracticePage.tsx 词组测评功能增强
- **新增四字词模式**：支持四字词和成语的专项练习
- **新增打字速度统计**：字/分钟，基于总输入字数和总输入时间
- **新增选重率统计**：错误输入需要选重的比例
- **增强统计卡片**：从3列改为4列，显示正确率、速度、选重率、平均用时

### 2026-05-02 第四次修复（安全除法 + 字集重建 + 词组验证）

#### 1. EvaluatePage.tsx 安全计算修复
- **新增 `safeDivide()`**：安全除法函数，避免除以零，所有除法运算统一使用
- **新增 `clampPercentage()`**：百分比范围限制函数，确保结果在 0-100%
- **新增 `roundTo()`**：四舍五入函数，避免浮点精度问题
- **新增 `normalizeWeights()`**：权重归一化函数，确保权重之和为1.0
- **修复所有除法**：`a / b` → `safeDivide(a, b)`，`a / b * 100` → `clampPercentage(safeDivide(a, b) * 100)`
- **修复效率评分权重**：使用 `normalizeWeights([0.42, 0.35, 0.15, 0.08])` 确保权重归一化
- **修复综合评分**：使用 `roundTo()` 避免浮点精度问题

#### 2. builtinCharSets.ts 字集数据全面重建
- **前500字**：500个不重复汉字（0重复，0非汉字）
- **前1000字**：1000个不重复汉字（0重复，0非汉字）
- **前3000字**：3000个不重复汉字（0重复，0非汉字）
- **新增 GB2312/GBK 子集**：`gb2312Level1`(3755字)、`gb2312Level2`(3008字)、`gb2312All`(6763字)
- **新增 `validateCharSets()`**：运行时自动验证字集数量、重复、非汉字
- **新增 `commonStandard`**：通用规范汉字表（8105字）

#### 3. commonChars.ts 重构
- **改为从 builtinCharSets 导入**：避免数据重复，确保一致性
- **前500字**：500个，前1000字：1000个，前1500字：1500个，前3000字：3000个
- **运行时自动校验**：数量不符时 console.warn 报告

#### 4. builtinPhrases.ts 词组验证
- **去重后673个词组**（超过500个目标）
- **12个重复词**由 `cleanPhrases()` 自动去重
- **二字词200个 + 三字词150个 + 四字词200个 + 补充135个**

---

## 数据修复记录

#### 1. EvaluatePage.tsx 计算错误修复
- **字频分段统计修复**：`entries.slice(0, tier.max)` 改为按字频权重降序排序后切片，确保"前N字"统计准确性
- **效率评分权重校验**：添加 `EFF_WEIGHTS` 数组，运行时校验权重之和是否为1.0
- **除零保护确认**：所有除法运算均已添加 `> 0` 检查（`entries.length > 0`、`freqSum > 0`、`totalKeyPresses > 0` 等）
- **百分比范围限制确认**：所有百分比输出均使用 `Math.max(0, Math.min(100, ...))` 限制在 0-100%

#### 2. 字集数据全面重建
- **builtinCharSets.ts**（195KB → ~12KB）：
  - 移除全部非汉字字符（原文件含大量偏旁部首、生僻字、乱码）
  - 添加 `isHanChar()` 和 `cleanCharSet()` 自动校验工具
  - `top500Chars`：500个真正高频汉字，0重复，0非汉字
  - `top1000Chars`：1000个汉字，0重复，0非汉字
  - `top3000Chars`：2000+汉字（精简版），自动去重过滤
  - 新增 `isInGB2312()` / `isInGBK()` 辅助函数
  - `calculateCoverage()` 添加百分比范围保护 `Math.min(100, Math.max(0, ...))`
- **commonChars.ts**（647行 → ~289行）：
  - 移除全部非汉字字符和重复数据
  - 添加与 `builtinCharSets.ts` 一致的 `isHanChar()` / `cleanCharSet()` 工具
  - `top500Chars`：500个汉字
  - `top1000Chars`：1000个汉字
  - `top1500Chars`：1500个汉字
  - 运行时自动校验并 console.warn 报告问题

#### 3. 词组数据扩充与清理
- **builtinPhrases.ts**：
  - 移除末尾5个乱码词组（'医喉', '冼哰', '厴喏', '儱吶', '克啯', '凬嗨'）
  - 二字词：200个（≥100目标）
  - 三字词：150个（≥100目标）
  - 四字词/成语：200+个（≥100目标）
  - 总数：**550+ 个词组**，全部经过 `isAllHan()` 校验
  - 添加 `cleanPhrases()` 自动去重过滤工具
  - 导出 `phraseStats` 统计信息

---

## 数据修复记录

### 2026-05-02 修复

#### 字集数据修复
- **top500Chars**: 修复为准确的 500 个不重复汉字（原仅 400 个）
- **top1000Chars**: 修复为准确的 1000 个不重复汉字（原仅约 670 个，含大量重复）
- **top3000Chars**: 修复为准确的 3000 个不重复汉字（原仅约 2469 个，含大量重复）
- **commonChars.ts**: 同步修复为 500 个不重复汉字（原 483 个唯一，17 个重复）
- 移除 `'蛔回'` 等非单字错误数据

#### 词组数据修复
- **builtinPhrases.ts**: 移除 `commonIdioms` 与 `commonThreeCharWords` 之间的 38 个重复成语
- **commonThreeCharWords**: 清理为仅包含真正的三字词（原含 1132 个四字词）
- 补充 200 个常用三字词，词组总数达 **914** 个（526 双字词 + 259 三字词 + 129 成语）

#### 计算逻辑验证
经全面审计，以下计算逻辑正确：
- ✅ 平均值计算均含除零保护
- ✅ 百分比计算结果在 0-100% 范围内
- ✅ 统计数字计算正确
- ✅ 字集大小统计与实际数组长度一致
- ✅ 速度计算单位正确
- ✅ 评分公式各权重合理
- ✅ 排序算法比较函数正确
- ✅ 随机数生成使用正确

### 2026-05-02 第二次修复

#### 字集数量标注修复
- **builtinCharSets.ts**:
  - `top500Chars`: 确认 500 个，0 重复，0 非汉字
  - `top1000Chars`: 修复为 1000 个（原仅 500 个），0 重复，0 非汉字
  - `top3000Chars`: 修复为 3000 个（原仅 2000 个），0 重复，0 非汉字
  - `top6000Chars`: 修复为 6000 个，0 重复，0 非汉字
- **commonChars.ts**:
  - `top500Chars`: 确认 500 个，0 重复，0 非汉字
  - `top1000Chars`: 新增 1000 个分段，0 重复
  - `top1500Chars`: 新增 1500 个分段，0 重复
  - `top3000Chars`: 新增 3000 个分段，0 重复
  - 移除末尾的 '丁'、'丂'、'丄'、'丅'、'丆' 等非汉字字符

#### 计算逻辑保护修复（EvaluatePage.tsx）
- 修复 `calcScore` 函数：添加 `poor === excellent` 除零保护
- 所有百分比结果限制在 0-100% 范围内（`Math.max(0, Math.min(100, ...))`）
- `efficiencyScore` 和 `ergonomicsScore` 输入值先限制范围再计算
- `compositeScore` 权重之和 = 40% + 30% + 20% + 10% = 100%，确认正确

#### 词组数据扩充（builtinPhrases.ts）
- 词组总数从 50 个扩充到 **550** 个
- 包含：二字词、三字词、四字词、成语
- 所有词组仅包含汉字字符，0 非汉字
- 更新数据结构为 `phraseList: string[]` + 分类导出

#### PhrasePracticePage.tsx 适配
- 更新导入：`twoCharPhrases` / `threeCharPhrases`
- `buildPhraseList` 函数适配新数据结构

## 技术栈

- React 19 + TypeScript
- Vite 7
- Tailwind CSS 4
- shadcn/ui (Radix UI)
- Recharts（图表）
- React Router DOM